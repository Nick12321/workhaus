import datetime
DATE = datetime.datetime.now()

class Item:
    def __init__(self, itemName, label, postalCode, area):
        self.itemName = name
        self.label = label
        self.postalCode = code
        self.area = area

    def addItem:
